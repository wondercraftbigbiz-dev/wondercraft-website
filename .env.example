# Econt demo/test API. Copy to .env.local and fill in.
# Server-only — do NOT add a NEXT_PUBLIC_ prefix, it would inline these into the
# client bundle.

ECONT_API_URL=https://demo.econt.com/ee/services
ECONT_USERNAME=
ECONT_PASSWORD=
