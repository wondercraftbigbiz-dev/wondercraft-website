# Copy to .env.local and fill in. Never commit real values.
#
# Supabase project "Wondercraft Website" (ppuxxxhflctkzahnvxje).
# Both are server-only. Do NOT prefix either with NEXT_PUBLIC_ — the service role
# key bypasses row level security and must never reach the browser.

SUPABASE_URL=https://ppuxxxhflctkzahnvxje.supabase.co

# Supabase dashboard -> Project Settings -> API -> service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=
